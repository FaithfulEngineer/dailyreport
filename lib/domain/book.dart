class Book {
  Book(this.id, this.type, this.reportdate, this.diary, this.email);
  String id;
  String type;
  String reportdate;
  DateTime reoportdated = DateTime.now();
  String diary;
  String email;
}
